version: '3.8'

services:
  backend:
    image: your-backend-image:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENV_VAR_NAME=your_value
    volumes:
      - .:/app